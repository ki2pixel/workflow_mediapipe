# DockerHub[](#dockerhub)

Deploy a private [DockerHub](https://hub.docker.com/) image using the following steps:

## Obtain a token[](#obtain-a-token)

1\. Go to your DockerHub personal access tokens page: https://app.docker.com/settings/personal-access-tokens

2\. Generate a new token with "Read-only" permissions:


Generating a new token

Select an Image

3\. Copy the generated token:


Once generated, copy the personal access token

Select an Image

## Start deployment[](#start-deployment)

When starting a deployment, click on the registry credentials and specify the DockerHub username and the generated token as your password. Then click "Deploy".


specify registry credentials

Select an Image

