# Suivi de Progression
- [2026-07-04 09:16:00] **Correction de la Résolution Contextuelle de WorkflowService (COMPLET)** : Résolution de l'erreur 'Execution context not available' lors du lancement d'étapes en recherchant dynamiquement `app_new` ou `__main__` dans `sys.modules`, garantissant un fonctionnement parfait en production directe et en exécution de tests.
- [2026-07-04 09:00:00] **Dépilage et Résolution de la Dette Technique Backend (Phase 1-4) (COMPLET)** : Implémentation du plan d'action de l'audit backend avec : 1. Création des enums typés (`StepKey`, `StepStatus`, `CSVDownloadStatus`) et forçage de `sys.exit` sur erreur de config en production. 2. Hardening des exceptions `ijson` en STEP6/7 et redirection de stdout/stderr vers `logs/step_*.log` avec tailing thread non bloquant pour éviter les deadlocks de `subprocess.PIPE`. 3. Refactoring complet en DAG par extraction des services `csv_downloader`, `csv_monitor`, `workflow_executor`, et `cleanup_monitor` pour supprimer les dépendances circulaires avec `app_new.py`. 4. Intégration de `cleanup_step_logs` pour nettoyer automatiquement les fichiers de logs obsolètes step_*.log (seuil 48h) et harmonisation de la suite de tests unitaires (365 tests passés au vert).
- [2026-06-17 10:05:00] **Correction OOM TransNetV2 CV5 (COMPLET)** : Implémentation du fallback dynamique CPU (ORTDNNNet) en cas d'erreur mémoire/CUDA (OOM) d'ONNX Runtime, et réduction du batch_size par défaut de 16 à 8 dans STEP3. Validation effectuée.
- [2026-06-14 20:35:00] **Mise à jour Coding Standards OpenCV 5.0 (COMPLET)** : Alignement du fichier `codingstandards.md` avec les récentes évolutions d'OpenCV 5.0 DNN (Graph Engine, multiprocessing spawn, limitation de threads, fallback ONNX/TFLite).
- [2026-06-14 20:30:00] **Création des skills OpenCV 5.0 (COMPLET)** : Création et intégration de `opencv5-dnn-engine-expert` et `cv5-multiprocessing-orchestrator` dans la base de connaissances pour documenter les spécificités du moteur de graphe et du multiprocessing sous CV5.
- [2026-06-14 20:25:00] **Harmonisation de la Documentation OpenCV 5.0 (COMPLET)** : Mise à jour et alignement de la documentation opérationnelle de détection de scènes (STEP 3) et de tracking (STEP 5) avec les spécifications et fonctionnalités du moteur expérimental OpenCV 5.0 DNN (Graph Engine, cascade d'inférence ONNX/TFLite, multiprocessing spawn avec isolation de threads).
- [2026-06-14 20:01:00] **Correction de format JSON, résolution ONNX et Détecteur Objets STEP5 CV5 (COMPLET)** : Résolution de trois problèmes identifiés sur le pipeline OpenCV 5.0 GPU/CPU : 1. Suppression du double backslash provoquant des sauts de ligne littéraux `\n` dans le JSON produit. 2. Correction de la résolution absolue des modèles ONNX dans `load_cv5_model` pour éviter la concaténation erronée avec le dossier par défaut. 3. Retrait temporaire du modèle `.tflite` de `os.environ` lors de la résolution via `ObjectDetectorRegistry` pour forcer le chargement de `yolo11n.onnx` en mode CV5.
- [2026-06-14 16:15:00] **Amélioration Progression UI STEP5 CV5 (COMPLET)** : Implémentation du suivi multi-ligne (`[Progression-MultiLine]`) pour le tracking en parallèle. Les workers ne polluent plus stdout directement mais passent par une queue IPC. Le parent agrège les informations, affiche la progression de chaque worker avec son index absolu et calcule le pourcentage moyen cumulé de l'étape. Ajout de `current_failure_line_pattern` pour faire avancer la barre de progression même en cas d'erreur.
- [2026-06-14 15:45:00] **Optimisation Multiprocessing & GPU STEP5 CV5 (COMPLET)** : Parallélisation du tracking via multiprocessing spawn (`STEP5_CV5_NUM_WORKERS=4`) et support GPU `CUDAExecutionProvider` dynamique. Résolution de l'incompatibilité de dimension d'entrée statique YOLOv11 (`yolo11n.onnx` en shape static `480x480`) par détection dynamique et correction spatiale. Vitesse mesurée de 5m 54s sur le lot de 8 vidéos (25% plus rapide que le TPU Edge classique séquentiel).

- [2026-06-14 14:12:00] **Intégration d'OpenCV 5.0 (COMPLET)** : Activation des pipelines expérimentaux OpenCV 5.0 pour STEP 3 (Scene Detection) et STEP 5 (Video Tracking) via `USE_OPENCV5_STEP3=true` et `USE_OPENCV5_STEP5=true` dans `.env`. Résolution de l'incompatibilité de `tflite-runtime` 2.14.0 avec NumPy 2.x (`AttributeError: _ARRAY_API not found` corrigé par rétrogradation de `numpy` à `<2.0.0` dans l'environnement virtuel `tracking_cv5_env`). Alignement et sécurisation de la validation et du parsing JSON dans `run_tracking_cv5.py`. Création et validation réussie de la suite de tests d'intégration unitaires (`test_step3_cv5_integration.py` et `test_step5_cv5_json_formats.py`) avec 100% de tests réussis.
- [2026-06-14 12:40:00] **Réorganisation des Documents de Recherche (COMPLET)** : Catégorisation des 16 fichiers markdown de recherche dans des sous-dossiers thématiques (`audio/`, `hardware/`, `models/`, `vision/`) sous `docs/recherches/` pour une structure plus propre.
- [2026-06-14 04:21:00] **Abandon et Nettoyage de l'Optimisation STEP3 Axe A (COMPLET)** : Suppression complète de l'Axe A (décodage GPU TorchCodec + torch.compile en FP16) après benchmarks révélant un goulot d'étranglement GPU séquentiel (27s par vidéo vs 22s en classique) et des décalages d'indices VFR. Nettoyage de l'environnement virtuel local `transnet_env`, des bibliothèques ffmpeg GPU et des modèles TensorRT/ONNX (libérant ~4 Go d'espace disque). Intégration de la méthode classique consolidée et validation par test suite.

- [2026-06-13 17:55:00] **Correction Annulation UI & Object Detector TPU (COMPLET)** : Résolution définitive du blocage de l'UI lors de l'annulation de la STEP5 (`SIGTERM` géré via `os._exit(0)` pour interrompre instantanément l'orchestration FFmpeg/Inférence sans deadlocks). Identification et correction du goulot d'étranglement de 35 FPS pour les vidéos sans visages : compilation locale de `detect.tflite` via `edgetpu_compiler` pour forcer le Fallback Object Detector sur l'Edge TPU. Le débit global cumulé a été validé à **82.2 FPS** sur 8 vidéos (~38k frames), validant et dépassant l'objectif initial de 60 FPS.

- [2026-06-13 14:17:00] **Optimisation STEP3 TPU (COMPLET)** : Implémentation du Producer-Consumer (threading/queue) et vectorisation JIT (Numba) pour la détection de scènes. Paramétrage FFmpeg optimisé (-threads 6, fast_bilinear, sws_dither=none).

- [2026-06-13 13:40:00] **Refonte Multiprocessing Zéro-Copie STEP5 TPU (COMPLET)** : Implémentation complète de la nouvelle architecture pour `run_tracking_tpu.py` ciblant >60 FPS sur Threadripper. Mise en place de `multiprocessing.spawn`, d'un Ring Buffer en `SharedMemory` pour l'isolation complète du pilote `libedgetpu`, et optimisation des I/O avec `orjson.OPT_APPEND_NEWLINE` et un pipe FFmpeg de 1MB. Validation théorique réussie, avec l'identification d'une défaillance matérielle sous-jacente (Segfault de `allocate_tensors`) liée à la version TFLite 2.17.1 du `coral_env`.

- [2026-06-13 13:30:00] **Migration ECAPA-TDNN vers ONNX Runtime (COMPLET)** : Remplacement du moteur TFLite par ONNX Runtime pour l'extraction d'embeddings vocaux dans la STEP4 (`run_audio_diarization_tpu.py`). Implémentation du *Dynamic Batching* (bufferisation par paquets de 32 spectrogrammes Log-Mel) et optimisation de la topologie NUMA du Threadripper (`intra_op_num_threads` dynamique, `ExecutionMode.ORT_SEQUENTIAL`). Temps d'exécution réduit de ~50% (de 4m43s à 2m20s pour 8 vidéos) via la suppression du goulot d'étranglement mémoire (*Memory-Bound Bottleneck*).

- [2026-06-13 11:48:28] **Parallélisation Hybride STEP4 (COMPLET)** : Implémentation du multiprocessing (ProcessPoolExecutor) inter-vidéo (via STEP4_MAX_WORKERS) et multithreading (ThreadPoolExecutor) intra-vidéo pour ECAPA-TDNN. Injection de multiprocessing.Lock pour l'accès au TPU et d'un singleton Thread-Local pour TFLite CPU.

- [2026-06-13 01:25:00] **Alignement et Harmonisation des Compétences/Règles TPU (COMPLET)** : Alignement scrupuleux de `@.agents/rules/codingstandards.md` et des fiches de compétences (`coral-tpu-hybrid-algorithms/SKILL.md` et `coral-tpu-model-compiler/SKILL.md`) avec l'implémentation réelle des scripts TPU (`run_tracking_tpu.py` et `run_audio_diarization_tpu.py`). L'utilisation par défaut du filtre One-Euro en STEP5, du clustering AHC cosine (seuil 0.32), de l'extraction de d-vectors ECAPA-TDNN sur CPU et du script de co-compilation SRAM sont désormais documentés. Validation via la suite de tests unitaires (23 tests passés pour STEP3 + One-Euro, 25 tests passés pour STEP4).
- [2026-06-13 01:20:00] **Harmonisation et Mise à Jour de la Documentation Coral TPU (COMPLET)** : Mise à jour de la documentation pipeline pour STEP3 (`03-scene-detection.md`) et STEP4 (`04-audio-analysis.md`) avec les détails des optimisations algorithmiques (GAP 1280D, Dugad, EMA, median filter, VAD 50% overlap, ECAPA-TDNN CPU extraction, clustering AHC). Ajout de la documentation détaillée de `CoralTPUOrchestrator` (`coral-tpu-orchestrator.md`) et mise à jour de l'index (`README.md`).

- [2026-06-12 16:15:00] **Conformité clé num_distinct_speakers_audio (COMPLET)** : Investigation et validation de la conformité structurelle STEP4. La clé est standard dans le codebase (requise par Lemonfox, Deepinfra et STEP5) ; sa présence sur TPU est donc conforme et conservée pour assurer la compatibilité descendante. Fichiers d'audit, walkthrough et scripts de comparaison mis à jour.
- [2026-06-12 16:08:00] **Analyse Comparative TPU vs GPU Post-Sweep (COMPLET)** : Validation quantitative et qualitative TPU vs GPU post-optimisations et post-sweep de paramètres effectuée. STEP3 : Nombre moyen de scènes ramené de 40,2 à 13,4 (cible GPU: 13,0), faux positifs réduits de 31,4 à 9,4 par vidéo (-70%), IoU Moyen en hausse à 0,4341 (+10,2% abs au total) et jitter moyen à 1,384s. STEP4 : Exactitude VAD à 74,18% (vs 54,80% initial, +19,38% abs), Rappel VAD à 83,50% (vs 54,89% initial, +28,61% abs) et F1-score à 83,62% (vs 65,63% initial, +17,99% abs). Résolution de la division fictive et distinction correcte des locuteurs (moyenne TPU 1,4 vs 1,6 GPU de référence). Rapport d'audit `docs/audits/audit_tpu_vs_gpu_camille.md` mis à jour.
- [2026-06-12 14:55:00] **Implémentation des Recommandations Audit TPU vs GPU Camille (COMPLET)** : Intégration complète des optimisations algorithmiques de l'audit `docs/audits/audit_tpu_vs_gpu_camille.md` dans `run_scene_detect_tpu.py` (STEP3) et `run_audio_diarization_tpu.py` (STEP4). STEP3 : Support modèle GAP 1280D (avec fallback logits 1000D), EMA sur embeddings (α=0.8 configurable via JSON), filtre médian 1D (remplacement de la moyenne mobile), seuillage adaptatif de Dugad (μ+k·σ, k=3.0, fenêtre M=25), twin-comparison pour transitions graduelles, timecode aligné `HH:MM:SS.mmm`. STEP4 : fenêtrage glissant 50% overlap (hop 0.48s), seuil VAD calibré 0.20 (compensation INT8), filtre médian sur probabilités, FSM Hangover 3 états (1.0s), clustering spectral adaptatif (eigengap + silhouette score, remplace n_clusters=2 fixe), section `speaker_stats` dans le JSON. Script `scripts/compile_mobilenet_gap.py` fourni pour compilation optionnelle. 38/38 tests STEP3+STEP4 passés. `codingstandards.md` mis à jour.
- [2026-06-12 14:24:00] **Audit et Rapport de Qualité Coral TPU vs GPU (COMPLET)** : Réalisation d'une comparaison quantitative et qualitative automatisée des artefacts de détection de scènes (STEP3) et d'analyse audio (STEP4) pour les 5 vidéos de "122 Camille". Rédaction du rapport d'audit détaillé `artifacts/audit_tpu_vs_gpu_camille.md` identifiant une sur-détection sur TPU (STEP3), une perte de rappel (VAD STEP4), et des écarts structurels, avec formulation de recommandations de tuning.

- [2026-06-12 14:13:00] **Séparation de l'activation TPU par étape (COMPLET)** : Ajout des variables `STEP3_ENABLE_CORAL_TPU`, `STEP4_ENABLE_CORAL_TPU` et `STEP5_ENABLE_CORAL_TPU` dans `.env` et `config/settings.py`. Mise à jour du routage dans `app_new.py` et des commandes dans `workflow_commands.py` pour vérifier conjointement le toggle master et l'étape. Tests unitaires ajoutés et validés.

> **Politique de conservation**  
> - Cette page liste les tâches terminées des ~90 derniers jours (depuis mars 2026).  
> - Les périodes plus anciennes sont résumées ci-dessous et détaillées dans `memory-bank/archives/progress_legacy.md`.

## Statut Actuel

- [2026-06-12 13:51:00] **Optimisations CPU de la Détection d'Objets STEP5 (COMPLET)** : Limitation à 1 thread de l'interpréteur TFLite CPU (pour éviter l'oversubscription), remplacement du redimensionnement d'image Pillow par OpenCV (cv2.resize en interpolation NEAREST) et implémentation du slicing Python (max 5 dées) justifié par le rapport de recherche.
- [2026-06-12 12:38:00] **Correction source détection et division par zéro STEP5 TPU (COMPLET)** : Harmonisation du champ `source_detector` à `"face_landmarker"` pour assurer la cohérence avec le pipeline standard. Sécurisation de la fonction `apply_affine_crop` dans `utils/tpu_facemesh_utils.py` avec une borne inférieure de `1e-5` sur `box_size` pour éviter tout crash de division par zéro.
- [2026-06-12 12:28:00] **Optimisation Pipeline Inférence Cascade STEP5 Edge TPU (COMPLET)** : Implémentation du multithreading asynchrone (Acquisition/Inférence/Sérialisation), co-compilation SRAM automatique pour BlazeFace et FaceMesh, remplacement strict de PIL par `cv2.warpAffine`, vectorisation des Blendshapes via `dict(zip)`, migration par défaut vers le filtre One-Euro, et sérialisation O(1) RAM via `orjson` streaming. Résolution des deadlocks de queue et des exceptions NumPy/pycoral liées à la version 2.0.
- [2026-06-12 03:00:00] **Correction du modèle fallback CPU STEP5 TPU (COMPLET)** : Résolution de l'erreur `TypeError: only length-1 arrays can be converted to Python scalars` de `pycoral.adapters.detect.get_objects`. Cette erreur était due au fait que `efficientdet_lite0.tflite` retourne des tenseurs d'anchors bruts sans l'opérateur de post-processing SSD. Remplacement par le modèle standard `detect.tflite` (SSD MobileNet V2) copié dans `assets/`, qui intègre l'opérateur `TFLite_Detection_PostProcess` attendu par l'API pycoral, assurant le bon fonctionnement du fallback CPU.
- [2026-06-12 02:27:00] **Correction progression UI STEP5 TPU & Optimisation Streaming (COMPLET)** : Fix de la barre de progression invisible (PYTHONUNBUFFERED=1 + branchement STEP5 sur handler STEP3). Optimisation performance : streaming JSON direct éliminant l'accumulation mémoire `results[]`. Tests validés (35/35).
- [2026-06-12 02:08:00] **Mise à jour Documentation Edge TPU (COMPLET)** : Harmonisation de la documentation via le workflow `docs-updater`. Intégration de l'accélération matérielle Google Coral Edge TPU pour la détection de scènes (STEP3), l'analyse audio/diarisation (STEP4) et le tracking vidéo (STEP5), ainsi que l'orchestrateur central (WorkflowService).
- [2026-06-11 21:45:00] **Harmonisation format audio JSON TPU (COMPLET)** : Modification de `run_audio_diarization_tpu.py` pour générer un fichier JSON frame-par-frame identique au format original (GPU) requis par la STEP6 (`json_reducer.py`) et la STEP7 (`preprocess_ae_json.py`). Extraction de la durée via `ffprobe` (ou fallback audio) et alignement des segments VAD sur la timeline à 25 fps. Mise à jour de la suite de tests unitaires Edge TPU.
- [2026-06-11 21:28:00] **Correction d'affichage des progressions TPU (COMPLET)** : Résolution de la double incrémentation (affichage (10/5) au lieu de (5/5)) pour la STEP3 et la STEP5 en TPU en supprimant les `print(...)` redondants dans les scripts Edge TPU qui dupliquaient les lignes de `logging.info` déjà redirigées vers stdout. Implémentation des configurations dynamiques de `progress_patterns` de la STEP4 pour prendre en charge l'accélération TPU.
- [2026-06-11 21:18:00] **Correction de l'extension de sortie STEP5 TPU (COMPLET)** : Modification de l'extension de fichier de sortie de `.blendshapes.json` à `.json` dans `run_tracking_tpu.py` pour assurer l'intégration transparente avec la compression d'artéfacts et les étapes suivantes (STEP6/7).
- [2026-06-11 21:10:00] **Correction TPU STEP5 & Lightbox Logs (COMPLET)** : Résolution de l'incompatibilité de l'argument `--videos_json_path` pour `run_tracking_tpu.py` en ajoutant son support argparse. Correction de l'UI des logs frontend en adaptant dynamiquement `specific_logs` de la STEP5 pour cibler `tpu_tracking*.log` lorsque l'accélération TPU est active.
- [2026-06-11 21:01:00] **Correction de l'extension de sortie STEP4 (COMPLET)** : Renommage des 5 fichiers de diarisation audio de `.json` à `_audio.json` dans `/home/kidpixel/workflow_mediapipe/projets_extraits/122 Camille/docs/` pour corriger le conflit d'extensions avec la STEP5 (tracking).
- [2026-06-11 20:44:00] **Correction YAMNet & Résolution TPU STEP 4 (COMPLET)** : Résolution des erreurs de dépendances et de modèle pour la STEP4 TPU dans `run_audio_diarization_tpu.py`. Installation des bibliothèques scientifiques `scipy` et `scikit-learn` dans le venv `coral_env`. Remplacement de l'URL YAMNet 404 par l'archive `PINTO_model_zoo` et implémentation de l'extraction automatisée du modèle `model_full_integer_quant.tflite`. Ajustement dynamique de la forme d'entrée à `[15360]` pour accepter les tranches audio de 0.96s. Intégration de la quantification manuelle des entrées `int8` et de la dequantisation des scores de parole et embeddings 1024D de sortie. Remplacement de `common.set_input` par `interpreter.set_tensor` pour contourner l'exception sur les tableaux 1D. Passage à 100% avec succès des inférences sur les 5 vidéos de test.
- [2026-06-11 19:25:00] **Centralisation et Synchronisation TPU-Frontend (COMPLET)** : Déplacement des scripts d'exécution TPU de `scripts/workflow_scripts/` vers `workflow_scripts/` afin d'uniformiser la résolution des chemins des modèles et des journaux avec le frontend Flask. Mise à jour de `settings.py` pour ajouter un fallback sur `BASE_PATH_SCRIPTS` si `VENV_BASE_DIR` ne contient pas l'environnement `coral_env`. Correction de la suite de tests unitaires TPU (Step 5) afin de résoudre correctement les imports locaux.
- [2026-06-11 19:17:00] **Implémentation des tests unitaires Coral TPU (COMPLET)** : Création de `test_coral_tpu_orchestrator.py`, `test_step3_scene_detect_tpu.py`, `test_step4_audio_diarization_tpu.py` et `test_step5_tracking_tpu.py` pour valider l'orchestrateur et les algorithmes de secours (CPU) compensant la quantification matérielle INT8. Les imports `pycoral` et `tflite_runtime` sont mockés pour s'exécuter dans un environnement de test CI standard. Tests exécutés et validés.
- [2026-06-11 19:08:00] **Création des Skills Coral Edge TPU (COMPLET)** : Implémentation des drafts de compétences dédiées à l'ASIC Coral (`coral-tpu-system-orchestration`, `coral-tpu-model-compiler`, `coral-tpu-hybrid-algorithms`) dans le répertoire `.agents/skills/`. Mise à jour de la matrice de routage dans `.agents/rules/skills-integration.md` avec priorité de niveau 2 pour isoler l'expertise système, de compilation INT8 et d'hybridation CPU.
- [2026-06-11 19:00:00] **Mise à jour Coding Standards (COMPLET)** : Refonte de `.agents/rules/codingstandards.md` pour inclure les contraintes d'architecture Google Coral Edge TPU (file séquentielle, modèles TFLite INT8, distance cosinus/filtre de Kalman) tout en condensant les règles ES3 pour respecter la limite absolue de 12 000 caractères (actuellement ~5117 caractères).
- [2026-06-11 18:50:00] **Intégration Expérimentale Google Coral Edge TPU (COMPLET)** : Déploiement d'une architecture hybride validant l'utilisation du Coral TPU (PCIe M.2) sans GPU dédié. Création de `coral_env` autonome. Orchestration asynchrone (`coral_tpu_orchestrator.py`) pour éviter l'éviction de cache de la SRAM (8 Mo). Migration complète des modèles lourds vers des architectures TFLite INT8 avec exécution TPU et lissages/traitements CPU dédiés : STEP3 (MobileNetV2 Siamois + Cosine Distance CPU), STEP4 (YAMNet VAD + Spectral Clustering CPU) et STEP5 (BlazeFace -> FaceMesh INT8 -> Blendshapes avec Filtre de Kalman vectorisé CPU). L'architecture standard reste active par défaut, le basculement s'effectuant via `ENABLE_CORAL_TPU_ACCELERATION` dans `.env`.
- [2026-06-11 13:15:00] **Validation Matérielle Google Coral M.2 TPU (COMPLET)** : Diagnostic matériel et logiciel du module Coral TPU sur architecture Threadripper X399. Contournement des bugs ASPM/AER via GRUB (`pcie_aspm=off pci=noaer`) et installation du pilote patché `feranick/gasket-driver` pour assurer la compatibilité avec le noyau Linux 6.8. Nœud `/dev/apex_0` opérationnel.
- [2026-06-04 13:15:00] **Validation Finale et Correction du Script de Tests STEP5 (COMPLET)** : Résolution d'une erreur de syntaxe/guillemets dans `scripts/run_step5_tests.sh`. Exécution réussie de la suite de tests STEP5 (32 tests passés, 1 skip) sous `tracking_env_slim` et validation du démarrage de l'application Flask via `validate_startup.py` (Validation PASSED).
- [2026-06-04 13:10:00] **Audit et Harmonisation de la Documentation et Suite de Tests (COMPLET)** : Harmonisation de `docs/workflow/ops/testing-strategy.md` (380 tests, tests STEP5, suite legacy). Correction de bugs de sérialisation JSON critiques (clés magiques non quotées) dans `json_reducer.py`. Correction des tests d'intégration et de l'isolation environnementale (`conftest.py`, `test_step5_json_formats`, `test_csv_monitor_no_retrigger`, `test_step2_fps_parsing`, `test_step4_lemonfox_toggle`).
- [2026-06-04 12:35:00] **Alignement Coding Standards et Environnements Virtuels (COMPLET)** : Alignement de `.agents/rules/codingstandards.md` avec l'état réel du repository (documentation de la STEP3, dépréciation de MySQL, et standardisation de `tracking_env_slim` pour MediaPipe CPU). Mise à jour et validation de la nomenclature des environnements virtuels dans les scripts de configuration (`setup_dev_environment.sh`), de diagnostics (`diagnose_tests.sh`, `validate_gpu_prerequisites.sh`), de test (`validate_tests.sh`), de test d'intégration (`test_step5_json_formats.py` avec résolution dynamique du python) et de configuration globale (`conftest.py`).

- [2026-06-03 20:16:00] **Correction Tests Historiques MySQL (COMPLET)** : Correction des erreurs de collection et d'exécution dans `tests/legacy/`. Mise à jour des imports vers `services.deprecated.mysql_service`, adaptation des fixtures pytest et des mocks `config` obsolètes, et ajout de skips explicites pour les tests obsolètes liés à `CSVService.test_mysql_connection()`. La suite legacy a été restaurée et validée avec succès.

- [2026-06-03 19:56:00] **Audit SonarCloud et Résorption de Dette Technique (COMPLET)** : Exécution d'un plan en 4 phases pour résorber 714 anomalies SonarCloud. Renforcement de la sécurité (variables globales JS, permissions `/dev/shm`, validation `event.origin`), fiabilisation (erreurs flottantes, Flask API), baisse de la complexité cognitive (`process_video_worker`, `json_reducer`) et modernisation ES11 (`globalThis`, `Object.hasOwn()`).

- [2026-06-03 15:05:00] **Benchmarking et Optimisation VRAM STEP3 (COMPLET)** : Modification de `run_transnet.py` pour supporter un paramètre CLI `--batch_size`. Création et exécution du script `scratch/benchmark_step3.py` évaluant les performances selon diverses valeurs de batch_size, mixed_precision, torchscript, et ffmpeg_threads. Identification de la configuration optimale pour les GPU limités à 4 Go (`batch_size=8`, `mixed_precision=true`, `ffmpeg_threads=1`). Les résultats ont été validés par les tests existants et intégrés dans `docs/workflow/pipeline/03-scene-detection.md` et `config/step3_transnet.json`.

- [2026-06-02 19:47:00] **Mise à Jour de la Documentation Technique (COMPLET)** : Alignement des guides du pipeline (`docs/workflow/pipeline/`) et de la sécurité (`docs/workflow/ops/security.md`) avec les optimisations récentes de l'architecture : architecture parallèle passe unique (STEP2), décodage asynchrone et batching O(1) RAM (STEP3), isolation GPU en sous-processus (STEP4), multiprocessing CPU O(1) RAM avec `cv2.setNumThreads(0)` (STEP5) et vérifications strictes via `validate_startup.py` (Production).

- [2026-06-02 19:41:00] **Audit et Mise à Jour des Coding Standards (COMPLET)** : Mise à jour du fichier `.agents/rules/codingstandards.md` pour y refléter les récents changements d'architecture : multiprocessing CPU O(1) avec `cv2.setNumThreads(0)`, transcodage passe unique STEP2, isolation GPU CUDA STEP4 en sous-processus et sauvegardes de démarrage strictes pour la production.

- [2026-06-02 19:37:00] **Audit et Optimisation de la STEP5 (COMPLET)** : Audit complet du multiprocessing MediaPipe sur CPU. Refonte de `workflow_scripts/step5/process_video_worker_multiprocessing.py` pour implémenter un buffer de réordonnancement asynchrone des chunks IPC et l'intégration de `StreamingJSONOutput`. Ces optimisations transforment l'empreinte mémoire d'un O(N) catastrophique en O(1) constant, tout en prévenant le thread thrashing via `cv2.setNumThreads(0)`.

- [2026-06-02 19:15:00] **Audit et Optimisation de la STEP3 (COMPLET)** : Refonte de `workflow_scripts/step3/run_transnet.py` pour implémenter la lecture vidéo asynchrone (Threading + Queue), gérer dynamiquement la liste des frames (pruning glissant pour maintenir la RAM en O(1)) et traiter l'inférence GPU par lots (Batch Size de 16). Ces optimisations résolvent le bottleneck I/O et la fuite mémoire, permettant un traitement continu et beaucoup plus rapide.

- [2026-06-02 19:06:00] **Audit et Optimisation de la STEP2 (COMPLET)** : Refonte de `workflow_scripts/step2/convert_videos.py` remplaçant l'architecture séquentielle à deux passes par un traitement parallèle en passe unique combinée. Implémentation du décodage matériel via `-hwaccel cuda`, parallélisation initiale via `ffprobe` et allocation jusqu'à 3 workers GPU simultanés pour NVENC. Intégration d'un audit prédictif des flux audio pour éviter le coûteux "fail & retry" et ajout d'un fallback CPU automatique sur `libx264` en cas de saturation de la VRAM.

- [2026-06-02 18:38:00] **Correction de la Segmentation Fault (SIGSEGV) STEP4 (COMPLET)** : Activation forcée de l'isolation GPU (`AUDIO_GPU_ISOLATION=1`) pour tous les profils GPU (y compris `gpu_fp32`) dans `.env`, `app_new.py` et `run_audio_analysis.py`. Cette modification résout le crash -11 survenant lors du traitement séquentiel en forçant le nettoyage de l'état CUDA/PyTorch à chaque nouvelle vidéo via l'exécution en sous-processus séparé.

- [2026-06-02 18:22:00] **Optimisation Pyannote Diarization (STEP4) (COMPLET)** : Résolution des fuites de mémoire VRAM/RAM dans `run_audio_analysis.py`. Ajout d'un cache global pour le chargement "lazy" des modèles d'embeddings (1 seule instance chargée). Enrobage de l'inférence d'embeddings dans `torch.inference_mode()`. Implémentation de l'isolation par sous-processus GPU (`AUDIO_GPU_ISOLATION=1`) qui lance l'analyse de chaque vidéo dans un sous-processus dédié (`--analyze_single_video`), garantissant la libération totale de la VRAM après chaque fichier. Ajout de nettoyages explicites (`gc.collect()`, `torch.cuda.empty_cache()`) et configuration optimisée de `PYTORCH_CUDA_ALLOC_CONF` (expandable_segments).

- [2026-06-02 17:45:00] **Compatibilité Pyannote 4.x (DiarizeOutput) (COMPLET)** : Résolution de l'AttributeError `'DiarizeOutput' object has no attribute 'itertracks'` survenu lors de l'exécution de la STEP4 avec les versions récentes de pyannote.audio (4.x+). Implémentation d'une détection et d'une extraction dynamiques de l'attribut `speaker_diarization` (l'objet `Annotation` sous-jacent) dans `run_audio_analysis.py` (modes principal et subprocess), avec compatibilité totale conservée pour les versions antérieures. Tous les tests de non-régression validés.
- [2026-06-02 15:22:00] **Configuration Transcodage Audio & Fallback Pyannote (COMPLET)** : Ajout des variables LEMONFOX_MAX_UPLOAD_MB, LEMONFOX_ENABLE_TRANSCODE, HF_AUTH_TOKEN et HUGGINGFACE_HUB_TOKEN au fichier .env pour résoudre l'erreur HTTP 413 sur Lemonfox et permettre le chargement des modèles Hugging Face lors du fallback Pyannote. Tests de régression Lemonfox/Pyannote validés (le fallback est fonctionnel).
- [2026-05-31 01:14:48] **Harmonisation de l'outillage Agent (COMPLET)** : Audit complet des règles, skills et workflows pour corriger les incohérences de dénomination des outils MCP. Remplacement de l'instruction de blocage `memory-bank` par `fast-filesystem`, correction des outils `fast_move_file`/`fast_create_directory` en `move_file`/`create_directory` (MCP `filesystem-agent`), et ajustement de `task-master-ai` vers `shrimp-task-manager` dans `v5.md`. Modification du protocole Memory Bank pour pointer vers `replace_file_content`/`multi_replace_file_content`.
- [2026-05-30 15:55:00] **Audit et Mise à Jour des Coding Standards (COMPLET)** : Mise à jour du fichier `.agents/rules/codingstandards.md` pour y refléter les récents changements d'architecture : streaming JSON O(1) (`ijson`, `StreamingJSONOutput`), sécurité de production, correctifs `DOMDiff` et retrait des dépendances Cloud.
- [2026-05-29 19:40:00] **Audit de Performance et Optimisation STEP5 (InsightFace GPU) (COMPLET)** : Audit complet des goulots d'étranglement InsightFace sur GPU 4 Go (GTX 1650). Implémentation de la restriction de modules, baisse de la résolution interne (det_size=480), et mise en place de l'export streaming JSON O(1) RAM. Résultat : vitesse accrue de 10 à 54+ FPS (+440%), réduction de VRAM sous les 800 Mo, et élimination complète des OOM sur CPU. Documentation pipeline et variables d'environnement actualisées.
- [2026-05-29 20:54:55] **Correction UI STEP5 (COMPLET)** : Suppression de la limite de lignes (`max-height` et `ellipsis`) sur `.progress-text-step` dans `steps.css` pour afficher correctement la liste complète des vidéos en cours de traitement.
- [2026-05-29 13:42:00] **Correction Bug Rendu DOMDiff (COMPLET)** : Résolution du bug de rendu de la liste de téléchargements locaux qui arrêtait le polling du frontend et empêchait l'apparition de la popup de workflow. Correction du passage direct des éléments `<li>` à `DOMDiff.morph` par le clonage du `<ul>` conteneur en amont.
- [2026-05-27 19:39:00] **Nettoyage configuration Vultr .env (COMPLET)** : Suppression complète de toutes les variables d'environnement Vultr obsolètes (sections `# STEP5 — Cloud Vultr` et `# STEP5 — Container Runtime`) devenues inutiles suite à l'abandon et à la non-fusion de la feature cloud dans la branche main.
- [2026-05-27 19:27:00] **Nettoyage des variables obsolètes (RENDER_*) (COMPLET)** : Suppression complète de `RENDER_REGISTER_TOKEN`, `RENDER_APP_CALLBACK_URL`, `RENDER_APP_CALLBACK_TOKEN`, `RENDER_REGISTER_URL_ENDPOINT`, `REMOTE_TRIGGER_URL`, `REMOTE_POLLING_INTERVAL` et du poller distant inutilisé (`RemoteWorkflowPoller`). Tous les tests unitaires et d'intégration ont été mis à jour et validés avec succès.
- [2026-05-27 19:13:12] **Persistance Sélective (AppState Middleware) (COMPLET)** : Implémentation de la recommandation de l'audit pour la persistance sélective de l'état frontend. Création d'un middleware déclaratif dans `AppState.js` avec migration automatique des anciennes clés. Refactoring complet de `main.js` et `eventHandlers.js` pour supprimer les accès directs au `localStorage` et restauration dynamique de l'UI des sélections d'étapes personnalisées. Ajout de `test_state_persistence.mjs` validé avec succès.
- [2026-05-27 19:02:00] **Intégration Virtual DOM (COMPLET)** : Implémentation du système Virtual DOM (DOM Morphing) léger via `DOMDiff.js` (O(N) diffing). Remplacement des `innerHTML` destructeurs dans la méthode `updateLocalDownloadsListUI` de `uiUpdater.js`. Ajout et passage complet des tests unitaires `tests/frontend/dom_diff.test.js` exécutés par le script `test:frontend`. Ceci résout la recommandation #1 de l'audit frontend.
- [2026-05-27 19:10:00] **Audit Frontend Complet (COMPLET)** : Réalisation d'un audit approfondi de l'architecture frontend. Analyse détaillée de l'immuabilité d'état (`AppState`), de la programmation par lots du rendu (`DOMBatcher`), du contrôleur réseau (`PollingManager`), du moniteur télémétrique (`PerformanceMonitor`) et de la barrière d'auto-guérison (`ErrorHandler`). Rédaction et publication du rapport complet dans `docs/audits/frontend_audit.md` en conformité avec les directives d'écriture authentique et sans empreinte AI.
- [2026-05-27 18:52:00] **Mise à jour Documentation - ijson & Startup Security (COMPLET)** : Harmonisation de la documentation via le workflow `docs-updater`. Intégration de la transition vers `ijson` (streaming O(1) RAM) dans les guides de STEP6 et STEP7, et documentation des safeguards de sécurité de production et de `validate_startup.py` dans le guide de sécurité.
- [2026-05-27 18:40:00] **Optimisation I/O pour STEP6/STEP7 (COMPLET)** : Implémentation d'un flux de lecture JSON asynchrone itératif (`ijson`) pour les scripts de réduction (STEP6) et de pré-traitement AE (STEP7). La consommation de la RAM passe de O(N) à O(1), résolvant la recommandation prioritaire d'architecture sur la gestion des projets très lourds. La compatibilité intégrale avec l'existant a été prouvée (17 tests passés). Walkthrough documenté.

### Historique synthétique (avant mars 2026)

1. **Décembre 2025 à Février 2026** — Intégration de Mistral Fine-Tuning, Refonte UI Timeline Connectée (Phases 1-3), décommissionnement VisualizationService, support avancé Media-Solution (Python bridge), et restriction/simplification des moteurs STEP5 (InsightFace GPU). Détails complets dans l'archive.
2. **Novembre 2025** — Finalisation de la migration `WorkflowState`, refactoring de maintenabilité, stabilisation STEP4 GPU et rationalisation des rapports. Détails complets dans `archives/progress_legacy.md`.
3. **Octobre 2025** — Hardening CSV/Dropbox, compression vidéo STEP2, refonte UX (mode compact unique, toggles téléchargements) et renforcement du pipeline STEP5/rapport HTML-only. Voir l'archive pour la chronologie.
4. **Septembre 2025 et antérieur** — Mise en place des standards v4.1 (A11y, XSS), diagnostics système, architecture services, isolation des environnements et gestion d’état frontend. Les entrées exhaustive sont conservées dans l’archive.
