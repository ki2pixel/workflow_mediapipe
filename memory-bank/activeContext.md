# Contexte Actif (Active Context)

## Tâche en Cours
Aucune.

## Objectifs
- Maintenir l'intégrité du repo.
- Préparer pour le développement futur.

## Décisions Récentes
- [2026-06-13 17:55:00] **Correction Annulation UI & Object Detector TPU (COMPLET)** : Modification du gestionnaire `SIGTERM` dans `run_tracking_tpu.py` pour utiliser `os._exit(0)` afin d'éviter le blocage asynchrone lors de l'annulation depuis l'UI. Compilation du modèle SSD MobileNet V2 (`detect.tflite` -> `detect_edgetpu.tflite`) permettant au Fallback Object Detector de s'exécuter sur TPU. Vitesse globale mesurée : 82.2 FPS cumulés.
- [2026-06-13 13:40:00] **Refonte Multiprocessing Zéro-Copie STEP5 (COMPLET)** : L'architecture de la STEP5 TPU a été refondue pour éliminer le GIL et les I/O bloquants. Séparation en `Producer` (FFmpeg) et `Consumer` (Inférence) via `multiprocessing.spawn`, avec communication zéro-copie utilisant `multiprocessing.shared_memory` (Ring Buffer). Optimisation de `StreamingNDJSONOutput` et `EnhancedSpeakingDetector` avec `orjson` en mode append_newline. Note : le pilote `gasket` / TFLite (v2.17.1) remonte actuellement un Segfault (`allocate_tensors`) ou `NumOutputs != 1` nécessitant une réinitialisation de `/dev/apex_0` ou un alignement des versions pycoral.
- [2026-06-13 13:30:00] **Migration ECAPA-TDNN vers ONNX Runtime (COMPLET)** : Remplacement du moteur TFLite par ONNX Runtime pour l'extraction d'embeddings vocaux dans la STEP4 (`run_audio_diarization_tpu.py`). Implémentation du *Dynamic Batching* (bufferisation par paquets de 32 spectrogrammes Log-Mel) et optimisation de la topologie NUMA du Threadripper (`intra_op_num_threads` dynamique, `ExecutionMode.ORT_SEQUENTIAL`). Temps d'exécution réduit de ~50% (de 4m43s à 2m20s pour 8 vidéos) via la suppression du goulot d'étranglement mémoire (*Memory-Bound Bottleneck*).
- [2026-06-13 01:32:00] **Correction Naming Step 1 (COMPLET)** : Modification de la logique de nommage unique dans `workflow_scripts/step1/extract_archives.py`. La fonction `compute_unique_project_dir` renvoie désormais le nom brut sans horodatage s'il n'y a pas de collision existante, et ajoute un suffixe d'horodatage ou un compteur uniquement en cas de conflit avec un dossier existant. Mise à jour et validation de `tests/unit/test_step1_unique_naming.py`.
- [2026-06-13 01:25:00] **Audit et Harmonisation Documentation TPU (COMPLET)** : Alignement scrupuleux des règles de codage (`codingstandards.md`) et des fiches de compétences (`coral-tpu-hybrid-algorithms/SKILL.md` et `coral-tpu-model-compiler/SKILL.md`) avec les choix de production réels : filtre One-Euro par défaut en STEP5, clustering AHC avec distance cosine (seuil 0.32) et extraction ECAPA-TDNN Float32 CPU en STEP4, et co-compilation SRAM partagée (8 Mo) dans le compilateur. Validation de l'intégrité du pipeline via la suite de tests unitaires (tests passés).
- [2026-06-13 01:20:00] **Harmonisation de la Documentation Coral TPU (COMPLET)** : Les documents de la STEP3 (`03-scene-detection.md`) et de la STEP4 (`04-audio-analysis.md`) ont été entièrement mis à jour pour correspondre à la réalité des algorithmes optimisés. Création d'une fiche technique complète pour le service de sérialisation `CoralTPUOrchestrator` (`coral-tpu-orchestrator.md`) et mise à jour de l'index de documentation générale (`README.md`).
- [2026-06-12 16:15:00] **Validation de la clé num_distinct_speakers_audio (COMPLET)** : Investigation de la conformité structurelle STEP4. La clé est standard dans le codebase (requise par Lemonfox, Deepinfra et STEP5) ; sa présence sur TPU est donc conforme et conservée pour assurer la compatibilité ascendante. Fichiers d'audit, walkthrough et scripts de comparaison mis à jour.
- [2026-06-12 16:08:00] **Analyse Comparative TPU vs GPU Post-Sweep (COMPLET)** : Évaluation quantitative des gains obtenus après la recherche par grille (grid sweep) de paramètres. STEP3 : nombre moyen de scènes ramené de 40,2 à 13,4 (cible GPU: 13,0), réduction de 70% des faux positifs (9,4 en moyenne), IoU Moyen en hausse à 0,4341 (+10,2% abs au total) et jitter moyen à 1,384s. Configuration optimale validée : EMA=0.85, Median=5, Dugad_K=2.6, Twin_H=2.5, Twin_L=1.8. STEP4 : exactitude VAD à 74,18% (vs 54,80% initial), Rappel VAD à 83,50% (+28,61% abs) et F1-score à 83,62% (+17,99% abs). Résolution de la division fictive et distinction correcte des locuteurs dans les dialogues (moyenne TPU 1,4 vs 1,6 GPU, seuil AHC=0.32). Rapport d'audit mis à jour.
- [2026-06-12 14:55:00] **Implémentation des Recommandations Audit TPU vs GPU Camille (COMPLET)** : Intégration complète des optimisations algorithmiques pour STEP3 (`run_scene_detect_tpu.py`) et STEP4 (`run_audio_diarization_tpu.py`). STEP3 : Support modèle GAP 1280D (avec fallback 1000D), EMA sur embeddings (α=0.8 configurable), filtre médian 1D (remplace moyenne mobile), seuillage adaptatif Dugad (μ+k·σ, k=3.0), twin-comparison pour transitions graduelles, timecode `HH:MM:SS.mmm`. STEP4 : fenêtrage glissant 50% overlap (hop 0.48s), seuil VAD calibré 0.20, filtre médian sur probabilités, FSM Hangover (1.0s, 3 états), clustering spectral adaptatif (eigengap/silhouette remplace n_clusters=2 fixe), section `speaker_stats` dans JSON. 38/38 tests passés sans régression. Script `scripts/compile_mobilenet_gap.py` fourni. `codingstandards.md` mis à jour.
- [2026-06-12 14:43:00] **Enrichissement de l'Audit Coral TPU (COMPLET)** : Intégration des conclusions du rapport d'expertise `docs/recherches/Optimisation-Coral-TPU-INT8.md` dans le rapport d'audit `docs/audits/audit_tpu_vs_gpu_camille.md`. Ajout des formulations mathématiques de seuillage adaptatif (modèle de Dugad modifié) et de lissage temporel (EMA, Median Filter) pour la STEP3, ainsi que de la machine à états de maintien (Hangover FSM) et du fenêtrage glissant chevauchant pour la STEP4.
- [2026-06-12 14:24:00] **Audit et Rapport de Qualité Coral TPU vs GPU (COMPLET)** : Réalisation d'une comparaison quantitative et qualitative automatisée des artefacts de détection de scènes (STEP3) et d'analyse audio (STEP4) pour les 5 vidéos de "122 Camille". Publication du rapport d'audit initial décrivant les anomalies de sur-détection et de perte de rappel.
- [2026-06-12 14:13:00] **Séparation de l'activation TPU par étape (COMPLET)** : Ajout des variables `STEP3_ENABLE_CORAL_TPU`, `STEP4_ENABLE_CORAL_TPU` et `STEP5_ENABLE_CORAL_TPU` dans `.env` et `config/settings.py`. Mise à jour du routage dans `app_new.py` et des commandes dans `workflow_commands.py` pour vérifier conjointement le toggle master et l'étape. Tests unitaires ajoutés et validés.
- [2026-06-12 13:51:00] **Optimisations CPU de la Détection d'Objets STEP5 (COMPLET)** : Limitation à 1 thread de l'interpréteur TFLite CPU (pour éviter l'oversubscription), remplacement du redimensionnement d'image Pillow par OpenCV (cv2.resize en interpolation NEAREST) et implémentation du slicing Python (max 5 détections) justifié par le rapport de recherche.
- [2026-06-12 12:38:00] **Correction source détection et division par zéro STEP5 TPU (COMPLET)** : Harmonisation du champ `source_detector` à `\"face_landmarker\"` pour assurer la cohérence avec le pipeline standard. Sécurisation de la fonction `apply_affine_crop` dans `utils/tpu_facemesh_utils.py` avec une borne inférieure de `1e-5` sur `box_size` pour éviter tout crash de division par zéro.
- [2026-06-12 12:28:00] **Optimisation Pipeline Inférence Cascade STEP5 Edge TPU (COMPLET)** : Implémentation complète de l'architecture asynchrone multi-thread (Axe 6), co-compilation matérielle (Axe 1), alignement `cv2.warpAffine` (Axe 2), vectorisation (Axe 3), filtre One-Euro par défaut (Axe 4) et streaming `orjson` O(1) (Axe 5). Correction du deadlock de queue et du bug de compatibilité Numpy 2.0/pycoral.
- [2026-06-12 03:00:00] **Correction du modèle fallback CPU STEP5 TPU (COMPLET)** : Résolution de l'erreur `TypeError: only length-1 arrays can be converted to Python scalars` survenue lors de l'exécution sur CPU du détecteur d'objets `efficientdet_lite0.tflite` (qui retourne des tenseurs d'anchors bruts incompatibles avec l'API pycoral). Remplacement par le modèle standard `detect.tflite` (SSD MobileNet V2) copié dans `assets/`, qui intègre l'opérateur `TFLite_Detection_PostProcess` requis, assurant le bon fonctionnement du fallback CPU.
- [2026-06-12 02:27:00] **Correction progression UI STEP5 TPU & Optimisation Streaming (COMPLET)** : Résolution de la barre de progression invisible lors de l'exécution de la STEP5 TPU. Root cause : stdout du subprocess Python block-bufferé (~8KB). Fix : ajout de `PYTHONUNBUFFERED=1` dans `process_env` (app_new.py). Ajout de STEP4/STEP5 au branchement spécial STEP3 pour incrémenter `progress_current` sur `PROCESSING_VIDEO`. Optimisation performance : remplacement de l'accumulation mémoire `results[]` par écriture streaming directe (`StreamingJSONOutput`) pendant le pipeline, éliminant ~1-2MB de copies intermédiaires par vidéo longue. Tests : 31/31 workflow_commands + 4/4 step5_json_formats.
- [2026-06-12 02:08:00] **Mise à jour Documentation Edge TPU (COMPLET)** : Harmonisation de la documentation via le workflow `docs-updater`. Intégration de l'accélération matérielle Google Coral Edge TPU pour la détection de scènes (STEP3), l'analyse audio/diarisation (STEP4) et le tracking vidéo (STEP5), ainsi que l'orchestrateur central (WorkflowService).
- [2026-06-11 21:45:00] **Harmonisation format audio JSON TPU (COMPLET)** : Modification de `run_audio_diarization_tpu.py` pour générer un fichier JSON frame-par-frame identique au format original (GPU) requis par la STEP6 (`json_reducer.py`) et la STEP7 (`preprocess_ae_json.py`). Extraction de la durée via `ffprobe` (ou fallback audio) et alignement des segments VAD sur la timeline à 25 fps. Mise à jour de la suite de tests unitaires Edge TPU.
- [2026-06-11 21:28:00] **Correction d'affichage des progressions TPU (COMPLET)** : Résolution de l'double incrémentation pour la STEP3 et STEP5 en TPU. Implémentation des configurations dynamiques de `progress_patterns` de la STEP4.
- [2026-06-12 01:48:00] **Optimisation majeure STEP5 TPU (COMPLET)** : Correction du bottleneck de "Context Switch" TPU. L'Object Detector ne s'exécute désormais qu'en "fallback" (si aucun visage n'est détecté), rétablissant ainsi la priorité "Face over Object" d'origine. Correction d'un bug de `continue` qui skippait les frames vides. Le temps de traitement devrait chuter drastiquement.
- [2026-06-11 21:18:00] **Correction de l'extension de sortie STEP5 TPU (COMPLET)** : Modification de l'extension de fichier de sortie de `.blendshapes.json` à `.json` dans `run_tracking_tpu.py` pour assurer l'intégration transparente avec la compression d'artéfacts et les étapes suivantes (STEP6/7).
- [2026-06-11 21:10:00] **Correction TPU STEP5 & Lightbox Logs (COMPLET)** : Résolution de l'incompatibilité de l'argument `--videos_json_path` pour `run_tracking_tpu.py` en ajoutant son support. Harmonisation de l'affichage des logs frontend en ciblant dynamiquement le pattern `tpu_tracking*.log` pour la STEP5 sous accélération TPU.
- [2026-06-11 21:01:00] **Correction de l'extension de sortie STEP4 (COMPLET)** : Renommage des 5 fichiers de diarisation audio de `.json` à `_audio.json` dans `/home/kidpixel/workflow_mediapipe/projets_extraits/122 Camille/docs/` pour corriger le conflit d'extensions avec la STEP5 (tracking).
- [2026-06-11 20:44:00] **Correction YAMNet & Résolution TPU STEP 4 (COMPLET)** : Résolution des erreurs de dépendances et de modèle pour la STEP4 TPU dans `run_audio_diarization_tpu.py`. Installation des bibliothèques scientifiques `scipy` et `scikit-learn` dans le venv `coral_env`. Remplacement de l'URL YAMNet 404 par l'archive `PINTO_model_zoo` et implémentation de l'extraction automatisée du modèle `model_full_integer_quant.tflite`. Ajustement dynamique de la forme d'entrée à `[15360]` pour accepter les tranches audio de 0.96s. Intégration de la quantification manuelle des entrées `int8` et de la déquantisation des scores de parole et embeddings 1024D de sortie. Remplacement de `common.set_input` par `interpreter.set_tensor` pour contourner l'exception sur les tableaux 1D. Passage à 100% avec succès des inférences sur les 5 vidéos de test.
- [2026-06-11 20:14:00] **Ajustement Seuil de Détection TPU (COMPLET)** : Le paramètre `threshold` dans `run_scene_detect_tpu.py` a été baissé de `0.25` à `0.05`. L'analyse statistique (Percentile 95 et 99) a démontré que la distance cosinus sur les embeddings MobileNetV2 INT8 varie de manière très subtile par rapport à la méthode logicielle (TransNetV2). Ce nouveau seuil permet d'obtenir un décompte de scènes très proche des extractions originales de référence.
- [2026-06-11 20:05:00] **Correction Numpy Cosine Distance (COMPLET)** : Résolution de l'erreur `ValueError: shapes (1,1001) and (1,1001) not aligned` lors du calcul de similarité entre les embeddings TPU dans la STEP 3. Le tensor en sortie de MobileNetV2 a une dimension de `(1, 1001)`. L'utilisation directe de `np.dot` tentait une multiplication matricielle qui échouait. La sortie est désormais systématiquement aplatie (`output.flatten()`) avant d'être ajoutée à la liste des embeddings, permettant à `np.dot` d'exécuter un produit scalaire valide sur des vecteurs 1D.
- [2026-06-11 20:02:00] **Mise à niveau libedgetpu.so (COMPLET)** : Résolution de l'erreur `EdgeTpuDelegateForCustomOp failed to invoke` survenue lors de l'appel à `interpreter.invoke()`. Cette erreur silencieuse était causée par une incompatibilité ABI entre `tflite-runtime 2.17.1` et la librairie officielle `libedgetpu1-std` de Google (compilée pour TF 2.5). Remplacement de la librairie officielle dans `coral_env/lib/` par la version maintenue par la communauté (`feranick/libedgetpu`) spécifiquement recompilée pour TF 2.17.1. L'inférence matérielle sur le TPU fonctionne désormais correctement.
- [2026-06-11 19:51:00] **Correction de l'API pycoral (COMPLET)** : Correction d'une erreur critique `module 'pycoral.utils.edgetpu' has no attribute 'load_delegate'` lors de l'initialisation du TPU. L'API correcte pour les versions récentes de pycoral est `load_edgetpu_delegate()`. Cette modification a été appliquée de manière préventive sur les scripts TPU des étapes 3 (`run_scene_detect_tpu.py`), 4 (`run_audio_diarization_tpu.py`) et 5 (`run_tracking_tpu.py`).
- [2026-06-11 19:45:00] **Correction de compatibilité NumPy/PyCoral (COMPLET)** : Résolution d'une erreur fatale lors du lancement de la STEP3 (`AttributeError: _ARRAY_API not found` et `SystemError: initialization of _pywrap_coral raised unreported exception`). Ces erreurs provenaient d'une incompatibilité ABI introduite par NumPy 2.x, alors que `pycoral` et `tflite_runtime` sont compilés pour NumPy 1.x. Le package NumPy a été rétrogradé à la version `1.26.4` (`numpy<2`) dans `coral_env`, restaurant le fonctionnement normal du script d'inférence.
- [2026-06-11 19:42:00] **Résolution du Runtime Edge TPU (`libedgetpu.so.1`) (COMPLET)** : L'erreur de chargement de bibliothèque dynamique lors de l'exécution de la STEP3 a été corrigée. Les bibliothèques précompilées `libedgetpu1-std` (v16.0) ont été téléchargées depuis le dépôt officiel et installées localement dans `coral_env/lib/` pour ne pas polluer l'hôte. Le lanceur de processus `app_new.py` a été patché pour injecter dynamiquement `LD_LIBRARY_PATH` pointant vers `coral_env/lib/` lors des appels `subprocess.Popen` pour les process TPU.
- [2026-06-11 19:25:00] **Centralisation et Synchronisation TPU-Frontend (COMPLET)** : Centralisation des scripts expérimentaux TPU de `scripts/workflow_scripts/` vers `workflow_scripts/` pour aligner la résolution des chemins des modèles (`assets/`) et des fichiers journaux (`logs/stepX/`) avec le frontend. Ajout du fallback de détection de l'environnement virtuel dans `settings.py` pour localiser `coral_env` à la racine si inexistant sur le disque externe. Correction et validation des imports dans la suite de tests unitaires TPU (Steps 3, 4 et 5).
- [2026-06-11 18:50:00] **Architecture Hybride TPU/CPU** : Intégration optionnelle d'un routeur asynchrone vers le TPU Coral activable via `.env`, isolant cette expérimentation des process de production standards. Les réseaux incompatibles ont été remplacés par des alternatives INT8 avec une gestion des compromis mathématiques (ex: Filtre de Kalman) effectuée sur CPU.
- [2026-06-11 13:15:00] **Validation Matérielle Google Coral M.2 TPU (COMPLET)** : Diagnostic matériel et logiciel du module Coral TPU sur architecture Threadripper X399. Contournement des bugs ASPM/AER via GRUB (`pcie_aspm=off pci=noaer`) et installation du pilote patché `feranick/gasket-driver` pour assurer la compatibilité avec le noyau Linux 6.8. Nœud `/dev/apex_0` opérationnel.
- [2026-06-04 13:15:00] **Validation Finale et Correction du Script de Tests STEP5 (COMPLET)** : Résolution d'une erreur de syntaxe/guillemets dans `scripts/run_step5_tests.sh`. Exécution réussie de la suite de tests STEP5 (32 tests passés, 1 skip) sous `tracking_env_slim` et validation du démarrage de l'application Flask via `validate_startup.py` (Validation PASSED).
- [2026-06-04 13:10:00] **Audit et Harmonisation de la Documentation et Suite de Tests (COMPLET)** : Harmonisation de `docs/workflow/ops/testing-strategy.md` pour l'aligner avec l'état réel (380 tests, tests STEP5 et suite legacy). Correction de bugs de sérialisation JSON critiques (clés magiques non quotées) dans `json_reducer.py`. Résolution des dépendances et de l'isolation dans la suite de tests (`conftest.py`, `test_step5_json_formats`, `test_csv_monitor_no_retrigger`, `test_step2_fps_parsing`, `test_step4_lemonfox_toggle`).
- [2026-06-04 12:35:00] **Alignement Coding Standards et Environnements Virtuels (COMPLET)** : Alignement de `.agents/rules/codingstandards.md` avec l'état réel du repository (documentation de la STEP3, dépréciation de MySQL, et standardisation de `tracking_env_slim` pour MediaPipe CPU). Nomenclature des environnements virtuels standardisée dans tous les scripts système, de test et de validation.
- [2026-06-03 20:16:00] **Correction Tests Historiques MySQL (COMPLET)** : Résolution des erreurs de collection et d'exécution dans la suite de tests unitaires et d'intégration legacy (`tests/legacy/`). Redirection des imports vers `services.deprecated.mysql_service`, remplacement des mocks `config` obsolètes par des injections explicites, et skip documenté des tests dépendant d'anciennes méthodes de `CSVService`. Suite de tests validée avec succès via `tests-suite-guardian`.

## Questions Ouvertes
- Aucune

## Prochaines Étapes
- Compilation optionnelle du modèle GAP 1280D via `scripts/compile_mobilenet_gap.py` si TensorFlow et `edgetpu_compiler` sont disponibles.